# Production knowledge

本番の画像生成GPTが必要時に参照する資料を置く。

予定ファイル:

- `character-identity-reference.md` — 基準画像と一致させる特徴
- `motion-vocabulary.md` — 状態名と視覚的な動作例
- `sprite-output-spec.md` — スプライト依頼時だけ使う出力仕様

実行命令やAction手順は置かない。Knowledgeは参照情報に限定する。
